# Epics — BrainTraining Reading Game

Epics are large features. Each epic is broken into user stories (see STORIES.md when created).
Status: [ ] Not started | [~] In progress | [x] Complete

---

## Epic 1 — Project Scaffold
**Goal:** Skeleton is in place. Every folder exists, every file has a home, docs explain everything. No feature code yet.

- [x] Create Next.js project
- [x] Install dependencies (lucide-react, openai)
- [x] Create all folder structure
- [x] Write README.md for every folder
- [x] Write project docs (ARCHITECTURE, GAME_LOOP, METRICS, EPICS)

---

## Epic 2 — Types, Registry & Game Engine
**Goal:** The TypeScript foundation is solid. Types are defined. Category registry is complete. Question scheduler logic works and is tested.

Stories:
- [ ] Define all types (story.ts, question.ts, session.ts, metrics.ts)
- [ ] Build category registry (lib/game/registry.ts) with all 8 categories
- [ ] Implement question scheduler (lib/game/questionScheduler.ts)
- [ ] Implement scoring utilities (lib/game/scoring.ts)
- [ ] Create mock data (lib/mock/stories.ts + lib/mock/questions.ts)
- [ ] Implement hooks stubs (useGameSession, useReadingTimer, useQuestionScheduler, useLocalStorage)

---

## Epic 3 — UI Foundation
**Goal:** shadcn/ui installed. Layout components built. Design tokens established. App shell renders correctly.

Stories:
- [ ] Install and configure shadcn/ui
- [ ] Build AppShell (nav + page wrapper)
- [ ] Build ReadingShell (full-screen, distraction-free)
- [ ] Build PageHeader component
- [ ] Set up global styles and typography

---

## Epic 4 — Game Setup Flow
**Goal:** User can navigate from dashboard → pick category → pick story count → start a session.

Stories:
- [ ] Build Dashboard page (mock data: stats bar, category stats, recent sessions)
- [ ] Build CategoryCard component
- [ ] Build StoryCountPicker component
- [ ] Build /game/setup page (wires both components)
- [ ] Navigation: tapping Start on setup redirects to /game/play with session params

---

## Epic 5 — Reading & Question UI
**Goal:** The full game loop works end-to-end with mock data. User can read stories, see memory prompts, answer questions, and reach the results screen.

Stories:
- [ ] Build ReadingView (paragraph-by-paragraph display + timing)
- [ ] Build MemoryPrompt overlay
- [ ] Build TypeInQuestion component
- [ ] Build MultiChoiceQuestion component (7 + not-applicable)
- [ ] Build MultiSelectQuestion component (7 + not-applicable)
- [ ] Build QuestionFeedback component
- [ ] Build QuestionOverlay (wrapper that dims story + shows question)
- [ ] Build ReadingTimer (optional visible timer)
- [ ] Wire /game/play page with useGameSession + all components
- [ ] Build /game/results page with SessionResults component

---

## Epic 6 — Story Library
**Goal:** Users can browse all stories and read them outside the game.

Stories:
- [ ] Build StoryCard component
- [ ] Build StoryReader component
- [ ] Build /stories page (grid of StoryCards, filter by category)
- [ ] Build /stories/[id] page (StoryReader, link to parent if continuation)

---

## Epic 7 — History & Settings
**Goal:** Users can see past performance and configure preferences.

Stories:
- [ ] Build /history page (recent sessions, accuracy trend, WPM trend)
- [ ] Build /settings page (timer toggle, session length default)

---

## Epic 8 — AI Story Generation
**Goal:** Real stories generated by GPT-4o every morning at 4AM.

Stories:
- [ ] Implement generateStories.ts (OpenAI structured output)
- [ ] Implement generateQuestions.ts (OpenAI structured output)
- [ ] Build /api/cron/generate-stories API route
- [ ] Configure Vercel Cron Job (4AM daily)
- [ ] Validate output shape matches TypeScript types
- [ ] Add generation logging

---

## Epic 9 — Database Integration
**Goal:** Replace all console.log('TODO save to db: ...') with real PostgreSQL writes/reads.

Stories:
- [ ] Set up Neon PostgreSQL instance
- [ ] Write and run schema migrations
- [ ] Implement lib/api/stories.ts (reads)
- [ ] Implement session save endpoints
- [ ] Implement question response save
- [ ] Implement metrics aggregation queries
- [ ] Swap all mock data imports for real API calls

---

## Epic 10 — Auth & User Accounts
**Goal:** Users can create accounts and their progress persists across devices.

Stories:
- [ ] Set up NextAuth.js or Clerk
- [ ] Add sign-up / sign-in UI
- [ ] Associate sessions with user_id
- [ ] Dashboard shows personalized data
- [ ] Streak tracking per user

---

## Epic 11 — Polish & Launch
**Goal:** App feels polished and is ready for real users.

Stories:
- [ ] Onboarding flow (first-time user explanation of the game)
- [ ] Streak display + streak recovery mechanic
- [ ] Performance trend charts (30-day WPM, accuracy)
- [ ] Empty states (no sessions yet, no stories today)
- [ ] Error handling (API failures, network issues)
- [ ] Mobile layout review
- [ ] Vercel deployment + custom domain

---

## Backlog / Future Epics

- **Adaptive difficulty** — automatically increase story count or questions when user is consistently scoring high
- **Constrained timer mode** — user must advance before time runs out (competitive)
- **Story favorites** — bookmark stories to revisit
- **Leaderboard / social** — weekly accuracy rankings
- **Category difficulty ratings** — some categories are harder (mystery requires more tracking)
- **Accessibility** — screen reader support, dyslexia-friendly fonts
